trigger:
- master

pr:
- master

variables:
  version: BETA

jobs:
- job: TestVar
  pool:
    vmImage: 'ubuntu-16.04'

  steps:
  - bash: |
      echo "Hello World"
  	  echo $(version) is the content
    displayName: Step Test

